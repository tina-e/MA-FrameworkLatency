For **PsychoPY** test applications, we created a `venv` since it requires `pyglet` version < 1.5
To run the test applications, create and activate a Python `venv` and install packages listed in `requirements.txt`.
